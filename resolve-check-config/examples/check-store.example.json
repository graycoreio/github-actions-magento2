{
  "$schema": "../check-store.schema.json",
  "jobs": {
    "unit-test": true,
    "coding-standard": true,
    "phpstan": true,
    "smoke-test": {
      "services": ["search", "queue", "cache", "web"],
      "probes": ["page", "graphql"]
    }
  }
}
