on:
  push:
    branches:
    - main
    paths-ignore:
    - "docs/**"
    - README.md
  pull_request:
    branches:
    - main
    paths-ignore:
    - "docs/**"
    - README.md

jobs:
  integration-workflow:
    uses: .github/workflows/integration.yaml@add_integration_test
    secrets:
      composer_auth: ${{ secrets.COMPOSER_AUTH }}